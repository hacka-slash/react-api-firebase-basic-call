..*The .map() function of arrays goes thru each element in the array and then returns to us whatever we specify to be returned.

..*Homework: Make a branch and instead of using axios, lets use 'superagent'?